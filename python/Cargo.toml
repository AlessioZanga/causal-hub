[package]
name = "causal-hub-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "causal_hub"
crate-type = ["cdylib"]

[dependencies]
ndarray = "0.15.6"
numpy = "0.20.0"
pyo3 = { version = "0.20.0", features = ["extension-module"] }
